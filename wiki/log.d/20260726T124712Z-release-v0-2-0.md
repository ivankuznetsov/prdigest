# 2026-07-26 — PRDigest v0.2.0 release preparation

Prepared the minor release that adds deterministic `prdigest facts`, optional
OpenAI-compatible `prdigest prose`, checkpoint-first prose delivery, and the
repository-owned ClawHub-ready OpenClaw skill.

The package version, changelog, local build/install examples, packaged-gem
smoke, container smoke tag, scheduler guidance, and supported documentation now
move together at v0.2.0. The deterministic `prdigest run` and
`prdigest-result` v1 contracts remain unchanged for Hive.

The coordinated Hive dependency update targets `prdigest ~> 0.2.0`, but cannot
become remotely installable until this exact gem is published. Preparation does
not create a tag, publish RubyGems or ClawHub artifacts, create a GitHub release,
merge either repository, or deploy anything.
