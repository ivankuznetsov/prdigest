# 2026-07-24 — PRDigest v0.1.1 release

Prepared the patch release that accepts Octokit `Time` values at the live
GitHub adapter boundary. The package version, build/install smoke expectations,
changelog, container examples, and supported v0.1.x documentation now move
together.

The clean gem-install and Docker smokes render a synthetic pull request whose
merge timestamp is a production-shaped `Time` object. This keeps the release
package boundary, rather than only a unit fixture, responsible for proving the
regression is fixed.

Publication remains an explicit operator-authorized step after exact-head CI,
local package verification, and immutable tag checks.
